# Arsenal map — task → skills (TEMPLATE — fill in your own skills)

Match the task's domain + trigger. Pull only what changes the outcome.
★ = token-saver (prefer these, keep on at all times).

## Token-savers — keep ON at all times
List the skills/tools that REDUCE tokens. Examples:
- ★ a compressed-output mode (e.g. caveman) — cut response tokens.
- ★ a command proxy that trims verbose CLI output.
- ★ compressed sub-agents for search / review / small edits (~60% smaller results).
- ★ a lean scraper (fewer tokens than the default).
- ★ a codebase knowledge-graph you QUERY instead of dumping whole files.
Rule: reach for the lean variant first, every time.

## <Domain 1 — e.g. Dev / code>
- <trigger / when> → <skill>
- ...

## <Domain 2 — e.g. Research>
- <trigger> → <skill>

## <Domain 3 …>
- ...

Keep it to the skills you actually installed. Group by domain, one trigger cue
per line, so the router can match a task in one pass.
